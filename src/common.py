# Import the itertools module
import itertools
import random
from itertools import combinations, permutations, chain
import io
import contextlib
from tqdm import tqdm  # Make sure tqdm is installed: pip install tqdm
from collections import defaultdict, deque
from typing import Dict, Set, Tuple, List, FrozenSet

__all__ = [
    'itertools', 'random', 'combinations', 'permutations', 'chain',
    'io', 'contextlib', 'tqdm', 'defaultdict', 'deque',
    'Dict', 'Set', 'Tuple', 'List', 'FrozenSet'
]
